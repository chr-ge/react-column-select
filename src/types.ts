export type Option = {
  value: string | number
  label: string
}

export type Theme = {
  primary?: string
  secondary?: string
  textColor?: string
  columnBgColor?: string
  buttonBgColor?: string
}
